diff --git a/node_modules/@deepseek-ai/dsh-llm-pi-ai/lib/index.js b/node_modules/@deepseek-ai/dsh-llm-pi-ai/lib/index.js
index 5c89b80..193f5b7 100644
--- a/node_modules/@deepseek-ai/dsh-llm-pi-ai/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-llm-pi-ai/lib/index.js
@@ -1,1 +1,2 @@
+import { reportExternalUsage } from "dsh-desktop-client-ui/billing-runtime";
 import { launchEnvironmentOf } from "@deepseek-ai/dsh-launch-environment";
@@ -1278,8 +1279,9 @@ function mapUsage(usage) {
 	};
 }
 function classifyPiAiError(message) {
-	if (/\b(?:401|403)\b/.test(message)) return "AUTH";
 	if (isQuotaExceededError(message)) return QUOTA_EXCEEDED_CODE;
+	if (/\b401\b/.test(message)) return "AUTH";
+	if (/\b403\b/.test(message)) return "FORBIDDEN";
 	if (/\b429\b|rate.?limit/i.test(message)) return "RATE_LIMIT";
 	if (/\b413\b|failed to buffer the request body:\s*length limit exceeded|payload too large|request body too large/i.test(message)) return "INVALID_REQUEST";
 	if (/\b400\b|invalid.?request/i.test(message)) return "INVALID_REQUEST";
@@ -1795,3 +1797,8 @@
 					signal: watchdog.signal,
-					headers: requestHeaders(profile.headers)
+					headers: {
+						...requestHeaders(profile.headers),
+						...options.sessionId !== void 0 ? { "x-deepseek-harness-session-id": String(options.sessionId) } : {},
+						...options.billingInvocationId ? { "x-opc-invocation-id": options.billingInvocationId } : {},
+						...options.billingContext ? { "x-opc-billing-context": Buffer.from(JSON.stringify(options.billingContext)).toString("base64url") } : {}
+					}
 				}), model.contextWindow, options.signal)[Symbol.asyncIterator]();
@@ -1798,2 +1805,3 @@
 				let exhausted = false;
+				let billingUsage = {};
 				try {
@@ -1805,4 +1813,6 @@
 							exhausted = true;
+							await reportExternalUsage(options, model.baseUrl, { ...billingUsage, cachedTokens: billingUsage.cacheReadTokens });
 							return;
 						}
+						if (result.value.type === "usage") billingUsage = result.value.usage;
 						yield result.value;
