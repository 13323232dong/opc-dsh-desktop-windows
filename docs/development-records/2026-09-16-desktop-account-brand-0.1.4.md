# 桌面账号信息插件编译产物修复记录

日期：2026-09-16

## 范围

桌面 Profile 将内置品牌插件升级至 `@opc/dsh-brand@0.1.4`。该版本通过插件打包编译门禁生成，包含桌面 preload 的当前账号读取逻辑。

## 验证要求

提交前验证 tarball 的账号组件含 `dshDesktopAuth.current()`；打包后必须在唯一正式 macOS App 的“设置 - 账号”页面确认不再显示“加载中”。
