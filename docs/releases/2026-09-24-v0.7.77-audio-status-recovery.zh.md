# Evan超级管家 v0.7.77 发布记录

- 日期：2026-09-24
- 平台服务端：`bbae866083a546ad15a2b2219908610e3ca22bc2`
- 平台 Release：`20260924084857-40dadd7f0754`
- 内置追爆插件：`@opc/dsh-viral-chase@0.1.53`

## 变更

- 音频生成中显示真实进度条、百分比和服务器后台执行说明。
- 音频任务失败时在卡片内展示服务端返回的明确失败原因，不再只留下空播放器。
- 服务端 Worker 启动时恢复已持久化但尚未完成的音频任务，复用原任务和预览标识，避免重复创建生产任务。
- MiniMax 返回 `1008` 时明确识别为供应商额度不足。

## 验证

- OPC API 定向测试：3 个文件、55 项测试通过。
- 追爆插件：构建、类型检查和 138 项测试通过。
- 插件包 SHA-256：`7c5ece467eeaf97afb82baf9c538f6cf18afd097a1fab7cd78641df958a7dde6`。
- 线上平台 Release `20260924084857-40dadd7f0754` 已部署并通过验证。

## 已知限制

MiniMax 正式账号实测返回 `status_code: 1008`（余额不足），备用 DMXAPI 通道也因预扣费额度不足返回 `insufficient_user_quota`。在供应商账户补足余额前，服务器不会产生 MP3；客户端会显示失败原因，不得将已提交状态视为生成成功。

本机没有有效的 Developer ID Application 证书，因此 macOS 产物属于未签名内测/验收包，不能作为公开签名发行包。
