# 0.7.51 追爆改写控件精简

- 平台基线：`f48ef36`，追爆插件 `@opc/dsh-viral-chase@0.1.38`。
- 变更：移除“原创文案与审核”卡中的品牌/业务背景、改写语气、目标时长和作品名称输入框；保留改写模式、可选自定义要求、正文编辑、执行改写、AI 法务审查和保存文案。
- 兼容：改写服务仍以固定默认值“自然、可信”和 15 秒执行，历史任务输入不迁移、不删除。
- 验证：插件测试 114/114、插件类型检查和构建通过；桌面测试 906/906、类型检查、品牌校验和应用编译通过。待从干净桌面 `main` 生成 macOS 本机验收包后补充安装验收信息。
- 限制：本机构建没有有效 Developer ID，产物仅可标记为未签名 macOS 本机验收包。
