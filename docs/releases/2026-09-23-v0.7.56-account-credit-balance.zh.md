# 0.7.56 商户算力余额读取修复

- 发布范围：桌面端主进程新增受控的 `desktop:credit-balance` IPC；账号页面通过该桥接从钥匙串保存的会话请求商户算力余额。会话令牌不传递到页面脚本。
- 插件：`@opc/dsh-brand@0.1.5`，artifact 为 `opc-dsh-brand-0.1.5.tgz`，SHA-256 为 `a56fbd955f21149d70413adc075683667deaa261b2032dfadbf81d9d9ddeb68b`。
- 验收目标：已登录商户在桌面端“账号”页面能显示具体积分；刷新按钮可重新读取余额；请求失败时给出可读错误提示而不是 `--`。
- 已知限制：余额依赖本机保存的有效商户会话和控制面服务可访问；会话过期时需重新登录。当前 macOS 构建没有 Developer ID 签名，仅作为本机验收包。
- 发布提交、最终产物 SHA-256 与真实 UI 验收截图将在本次构建、覆盖安装和验收完成后补充。
