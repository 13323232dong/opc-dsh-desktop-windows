# Evan超级管家 v0.7.67 发布记录

日期：2026-09-23。范围：macOS 桌面端与内置 AI 客服插件。

## 变更

- 内置 `@opc/DSH-ai-customer-service` 升级至 `0.1.28`。
- 冷启动时，遗留的未完成微信发送任务不再自动执行：发送中的任务记录为 `WECHAT_SEND_INTERRUPTED`，排队任务记录为 `WECHAT_SEND_RESUME_REQUIRED`。
- Profile manifest、桌面 bootstrap 与 release manifest 使用同一 artifact 和 SHA-256。

## 已验证事实

- 插件 `build -> test`：115/115 通过；类型检查通过。
- 插件 artifact：`opc-DSH-ai-customer-service-0.1.28.tgz`，SHA-256 `b8442b9dc498cc43ff36f4cf7c33965c7fc3b4706d419c5c4f900149c5483b36`。
- 冷启动回归测试确认遗留 queued 任务不调用微信发送适配器。

## 待安装验收

- 正式 App 冷启动后，AI 客服标签显示“已暂停 / 尚未扫描”；未点击开始监听。
- 只读检查没有执行微信搜索、切换聊天、输入或发送。微信一度弹出此前磁盘满载导致的存储空间警告，清理失败构建半成品后恢复正常聊天窗口；该警告不归因于 AI 客服。
- 用户明确开启监听后的真实未读草稿写入仍未验收；本记录不把扫描开启或模型生成等同于草稿写入成功。

## 产物与安装

- 桌面源码 commit：`189b623b2a48078b692692672184571e6cc0801a`，与 `fork/main` 同步。
- 已覆盖安装：`/Applications/Evan超级管家.app`，`Info.plist` 为 `0.7.67`。
- DMG：`Evan超级管家-mac-arm64.dmg`，SHA-256 `82b56aa57d07379830e73e799b7a3dcbccd5323b77409c6538c3e5c4698b3f26`。
- ZIP：`Evan超级管家-mac-arm64.zip`，SHA-256 `51cebb1ea257d59bf8576a015ac3e3413e01f9ed387468029156cd7b93890cf6`。
- 本机没有有效 Developer ID，产物未签名，仅用于本机正式应用覆盖和验收，不是公开签名发行包。
