# Evan超级管家 v0.7.63 发布记录

## 范围

桌面端“轨迹”新增扣费明细：显示当前对话和已可靠关联的 Agent Teams 子任务的实扣、预扣、退款、计费依据及轨迹跳转。平台模型调用、算力预扣结算、一键追爆付费步骤和自带 Key 的服务商结算记录均使用独立计费关联信息；普通对话分叉不会被当作子任务合并。

## 对应服务端

- 控制面主线提交：`8c595da`（含数据库增量和 `GET /api/v1/compute/charge-details`）。
- 一键追爆插件主线提交：`ba36f62`，内置 artifact 版本 `0.1.46`。

## 已验证

- 桌面定向测试 50 项通过，TypeScript 类型检查通过。
- 一键追爆插件构建、135 项测试和类型检查通过；artifact 包内名称与版本为 `@opc/dsh-viral-chase@0.1.46`。
- 控制面定向测试 46 项通过，构建和 Prisma schema 校验通过；数据库集成测试需要发布环境 PostgreSQL 后执行。

## 发布状态

此记录在正式 macOS/Windows 打包前创建。最终产物哈希、线上 Release ID、安装验收和自动更新验收将在发布门禁完成后补充。
