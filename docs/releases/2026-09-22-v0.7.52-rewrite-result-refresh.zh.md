# 0.7.52 追爆最新改写结果回填

- 平台基线：`28ada8e`，追爆插件 `@opc/dsh-viral-chase@0.1.39`。
- 修复：改写结果不再仅按文本内容去重；每个服务端 `executionId` 完成后，原创文案输入框都会覆盖为该次最新 `finalText`，包括两次生成文本相同的情况。
- 保护：排队、失败、取消或空结果不会覆盖用户正在编辑的正文。
- 插件 artifact SHA-256：`2451940b75a31007a1b15218e70466a28dd44daf4714e5bf616cea41199120f0`。
- 验证：桌面全量测试 `906/906`、类型检查、品牌校验、macOS ARM64 打包和包内容检查均通过。2026-09-23 在已安装的 `Evan超级管家.app`（`0.7.52`）中，以同一次真实“文案改写”执行验收：进度从 `0%` 到完成，原创文案输入框从旧占位文本更新为本次服务端生成的 `finalText`，并显示“本次 AI 改写结果”。截图：`artifacts/ui-acceptance/rewrite-latest-result-0.7.52.png`。
- 限制：本机没有有效 Developer ID，产物仅作为未签名 macOS 本机验收包。
