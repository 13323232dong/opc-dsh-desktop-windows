# 微信系统会话排除 0.1.17

日期：2026-09-20；来源：用户要求公众号、服务号、文件传输助手不用扫描。

- 插件 main `ab26176`；桌面 main `198fc7f`。功能分支 `codex/wechat-passive-polling` 和 `codex/bundle-wechat-passive-polling` 已推送。
- 原生列表过滤中英文公众号、订阅号、服务号聚合入口和文件传输助手。服务层按精确名称及可用的账号类型复核；旧队列在读取联系人前标记 skipped。不根据消息正文关键字过滤普通联系人，不改变显式发送工具。
- 限制：微信原生列表没有可靠提供所有独立品牌账号的账号类型；没有类型标识的独立服务号不能承诺全部识别。界面明确说明该边界。
- 回归：82 项插件测试、902 项桌面测试、9 项最终 Profile 制品检查通过，类型检查及插件构建通过。测试先复现旧代码未排除和旧队列继续打开的问题。
- 插件制品 SHA-256：`93d7eb7c1c29ffebc75d26360b3209844855153790ad24053299b0cbd5fd7a90`。
- 本机安装沿用唯一 `/Applications/Evan超级管家.app`。保留用户数据，未发消息；未签名本地包，不是远程公开发布。
- 已通过 macOS 包内门禁并覆盖正式 App；已安装插件与源码 tgz 逐字节一致。DMG SHA-256 `2a36fc0de887a4cba19967d2f5885b022f5303ea4e5d7e2a4971e801b680cd36`；ZIP SHA-256 `f0e0ae36b5fca9ecaa17a647b4c2dadb2abdbfcfee843c1c21d381aaee1e6cea`。路径沿用 release main 的 dist 固定产物。重复构建 App 移入废纸篓，可恢复。
- UI 验收未通过：工作台启动出现空白，冷启动及重新加载仍未恢复；期间应用进程再次变化，原因未确认。最终本机插件状态接口可读，返回 WECHAT_LOGIN_REQUIRED、监听 disabled。没有开启监听或发送消息，不把接口正常当成界面验收成功。需后续核查工作台客户端与微信登录状态。
