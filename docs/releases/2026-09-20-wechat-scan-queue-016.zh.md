# 微信未读扫描队列 0.1.16 本机交付

日期：2026-09-20。来源：用户反馈新消息没有及时识别，并要求继续优化。

## 源码和检查

- 插件 main：`c6fea9c`；桌面 main：`28a05c9`，均已推送。独立功能分支为 `codex/wechat-passive-polling`、`codex/bundle-wechat-passive-polling`。
- 插件 80 项测试通过、类型检查通过、原生 helper 与客户端构建通过、插件依赖审计无漏洞。
- 桌面 120 个测试文件、902 项测试通过，类型检查通过。启动器、Profile、安装包 manifest 均绑定 0.1.16；版本一致性检查曾发现启动器遗漏，修正后完整重跑通过。
- 制品 `opc-DSH-ai-customer-service-0.1.16.tgz` SHA-256：`c4be537624112aa6b6e1f1d810350f14cde6219613da123778500256e32be9ab`。
- 独立审查复现同会话快照重复生成草稿，已添加失败测试并改为合并尚未处理的同会话快照。

## 行为及边界

- 每次定时 tick 可独立扫描，模型生成不持有微信操作锁；微信读写仍串行。状态查询不等待操作队列。扫描与队列状态分别显示。
- 只裁剪已完成历史，保留全部待处理记录；同会话连续未读更新保留队列位置。
- 当前原生操作仍可耗时至 helper 超时（45 秒），因此不保证硬性五秒扫描。标题不匹配、消息方向未知、模型不可用、视觉供应商不可用仍明确报错，不以放宽联系人校验伪造成功。
- 本次仅本机打包安装，不发布服务器、远程更新或 Windows 制品。缺少有效 Developer ID 证书，产物为未签名本地包。
- 桌面既有 PPT 依赖 `image-size` 及其三层依赖存在四项 high 审计告警（ICNS/JXL/HEIF 解析拒绝服务）；本次无依赖变更，未声称消除此风险。

## 安装验收

- 已从干净同步 main 完成 macOS arm64 打包、品牌和包内发布契约检查。覆盖 `/Applications/Evan超级管家.app`，刷新 LaunchServices、Dock，正式应用进程 PID 90377，未删除账号数据。
- 已安装 tgz 与源码制品逐字节一致。真实桌面 UI 打开 AI 客服，点击“开启全局监听”后显示已连接、待处理数及当前任务。草稿队列未清空时扫描时间从 17:10:59 推进至 17:11:38；阶段随后为写入输入框，状态接口 2.55 秒返回。截图已在本任务展示（含私人会话，不入仓库）。确认不再等整批草稿执行完才扫描，但单次 UI 操作仍使实测间隔超过五秒。
- 本次未人为发送测试消息，未把旧记录的 drafted 状态当作本轮成功。新来信从到达到入队的端到端延迟、完整草稿回读未完成定量验收。
- DMG：`/Users/mac/opc-dsh-desktop-release-main/dist/Evan超级管家-mac-arm64.dmg`，SHA-256 `0745c9a435d7ebb2b26f9728701df17a0882d2a8bf8ae4395b87b8812672dff2`。
- ZIP：`/Users/mac/opc-dsh-desktop-release-main/dist/Evan超级管家-mac-arm64.zip`，SHA-256 `7163a94808d27f68eaf2e2d603d4d6f910ccdb3d6d0c2a8d736ed4b853e95384`。
- 已安装 App 主可执行文件 SHA-256：`1af684f056a8eb13e49fbd677072e437316086b076e3b9b92de3ddb343edc5b1`（不是整个目录哈希）。构建目录重复 App 移至废纸篓，可恢复；只保留正式入口。
