# AI 客服记录可见性 0.1.20

日期：2026-09-21。范围：本机 macOS AI 客服插件和桌面 Profile。

- 已验证根因：联系人接口显式加载成功后，前端仍使用被动状态的 `accountId` 控制渲染；被动探测未确认账号时，真实返回的空列表或联系人卡片均被隐藏。
- 修复：以本次显式加载成功状态展示结果，显示已学习联系人数；0 位时明确提示先学习联系人。
- 全局监听关闭时清除旧 `failureCode`，界面也只在监听开启时展示预览错误。历史 `WECHAT_ACCOUNT_UNAVAILABLE` 不再冒充当前异常。
- 发送任务增加“清除失败记录”，只删除 `failed`；保留成功、排队、发送中和结果待核验任务。
- 插件提交 `d5aa38f`，105 项测试、类型检查、构建和依赖审计通过。桌面 120 个测试文件、903 项测试及类型检查通过。
- 插件 artifact SHA-256：`16582f4c916808f2f93f9156c4d1fc41804d5b9271c5dc9f026f1ebf708e6250`。
- 已从干净桌面 `main` 构建并覆盖 `/Applications/Evan超级管家.app`。本次只做本机更新，不发布服务器或自动更新。
- 真实数据清理返回 `removed: 4`、`remaining: 1`：4 条失败发送任务已删除，唯一保留记录为邹敏 `AI-SEND-20260918-C`，状态“客户端已发送”。
- 冷启动真实 UI 验收通过：4 条失败发送任务没有恢复；全局监听保持关闭；旧的顶部“预览异常：WECHAT_ACCOUNT_UNAVAILABLE”没有恢复；未发送任何微信消息。
- 联系人学习存储当前没有 `styles.json`，因此没有已成功学习的联系人。真实点击“加载已学习联系人”后，微信账号核验返回 `WECHAT_ACCOUNT_UNVERIFIED`，请求未取得联系人列表；该状态不能表述成“已加载但记录被隐藏”，后续需单独修复微信账号稳定识别。
