# Evan超级管家 0.7.45 本机验收记录

日期：2026-09-22

## 交付内容

- 内置 `@opc/dsh-viral-chase` 升级至 `0.1.33`，在“上传自己的音色”中支持 MP4、MOV。
- 视频只在服务端短暂提取音轨并转换为 WAV 用于声音克隆，不会作为数字人素材保存；提取或供应商失败时清理视频和临时音频。
- Profile artifact、启动器映射、发布清单和升级测试均固定到新包，避免已安装账户继续加载 `0.1.32`。
- 平台 `main`：`79b6b30`；桌面候选版本：`0.7.45`。

## 验证范围

- 插件构建、类型检查与完整测试通过；后端语音克隆定向测试覆盖视频转音轨、无音轨失败和临时文件清理。
- 本地桌面打包、安装后界面验收与产物哈希在本次发布流程完成后补充。

## 已知限制

- 本机没有有效 Developer ID Application 证书，macOS 产物仅可标记为未签名本机验收包，不能作为公开分发版本。
