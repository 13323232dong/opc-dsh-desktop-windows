# Evan超级管家 0.7.80 Windows 登录同步候选

日期：2026-09-25。来源任务：`01a0d899-c5c9-7253-895c-cbc32a074b89` 的续接。

## 范围与来源

- 基于远程主线 `ae7c4cc`，继承完整 Evan 登录页、账号历史、密码显隐、记住密码及商户注册。旧 `0.7.56` 功能分支缺失 PPT 依赖，不再作为打包来源。
- 功能分支 `codex/windows-release-continuation-20260925`，修复提交 `01c2983`，已通过无冲突合并预览。
- 修复切回登录页时未取消旧工作台导航，以及 Cookie 清理结束后过期导航重新加载工作台的问题。
- 主线统一递增至 `0.7.80`，同步根锁文件与 Windows Profile release manifest。插件内容未变更。

## 验证

- 定向回归：58 项通过；真实导航函数的延迟 Cookie 测试修复前失败、修复后通过。
- 本地完整测试：131 个文件、946 项通过（单 worker、测试超时 30 秒）；默认并发首轮遇到既有 Git/PPT CLI 超时。
- TypeScript 类型检查、`git diff --check` 和独立代码审查通过。
- 基准主线 Windows 原生 CI `36137847971` 构建通过；最终 `0.7.80` Windows CI [36139126302](https://github.com/13323232dong/opc-dsh-desktop-windows/actions/runs/36139126302) 通过：130 个测试文件通过、1 个跳过，944 项通过、2 项平台跳过，类型检查、打包与包内校验通过。
- `npm audit` 报告既有 4 项 high（dsh-ppt、dsh-ppt-composer、image-size、pptxgenjs），本次未修改依赖，未声称已消除。

## 交付状态

- 构建来源：干净远程 `main@0bff9591d44314b06b8dfc248100ad58c7e0beac`。
- 固定产物：`dist/Evan超级管家-windows-x64-setup.exe`，287,360,343 字节。
- EXE SHA-256：`17256b25606f41677eed300e63acc72f68010e36b4e99a903caf2b0e47ca08f1`，与 Windows Runner 输出一致。
- GitHub artifact ZIP SHA-256：`bf37bca27d7cb81a94b6f1c8b1cd8745ca199982bed5b5d792d319915902bbfc`，与 GitHub API 记录一致。
- 已从 EXE 内 `$PLUGINSDIR/app-64.7z` 实际提取登录页、Profile、主进程代码和内测文案。登录页关键控件齐全；Windows checkout 使用 CRLF，标准化为 LF 后与本地主线源码完全一致，标准化 SHA-256 为 `f8829609789fd524e246555ce6bfdcf6fa690b3adb41a332133a124ee2720f9d`。
- Profile 20 个声明插件的原始字节和 SHA-256 均与构建源码一致。应用/manifest/更新元数据均为 `0.7.80`；`latest.yml` 的 SHA-512 与实际 EXE 匹配；包内存在 `js-yaml/package.json` 和 Evan 内测声明。
- 候选包 PE Authenticode 证书表为空，尚未执行 UKey 签名。签名后必须重新生成更新元数据和哈希。
- Windows 真机登录/注册、错误状态与重启验收未完成。未更新官网/更新源，未创建线上 Release 或切换部署，未覆盖本机 macOS App，未生成 macOS DMG/ZIP。
